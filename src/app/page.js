
import MainPage from "@/screens";

export const metadata = {
	title: process.env.NEXT_PUBLIC_APP_NAME + " - मराठी बातम्या",
	description: process.env.NEXT_PUBLIC_APP_NAME + " - मराठी बातम्या",
};

const HomePage = () => {
	return (
		<MainPage />
	);
}

export default HomePage;

